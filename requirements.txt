# Core (required to run the demo + tests)
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
pytest>=7.4

# Dashboard (optional)
plotly>=5.18
streamlit>=1.30

# Web API bridge (optional — serves the engine to a React/TypeScript frontend)
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27          # FastAPI TestClient (used by the API tests)

# Decision-brief export (offline): one-page PDF + Word download (/brief/export)
python-docx>=1.1
reportlab>=4.0

# LLM planner + narrator: Claude Sonnet 4.6 via the Anthropic API.
#   1) pip install (this line)   2) export ANTHROPIC_API_KEY=...
#   3) WAITCOST_PLANNER=claude (or auto). Falls back to deterministic rule-mode if
#      no key is set or the API is unreachable — so the demo never breaks.
anthropic>=0.40

# Stretch / production AI components (not needed for the offline demo)
# pymc>=5.10          # Bayesian calibration of transition rates
# numpyro>=0.13       # (alternative) JAX-based Bayesian inference
# scikit-learn>=1.3   # ML inflow predictor (we ship a NumPy implementation)
# xgboost>=2.0        # ML inflow predictor (alt, if the panel grows)
# shap>=0.44          # explainability (we compute exact additive SHAP in NumPy)
# optuna>=3.5         # allocation optimization
